import 'package:flutter/material.dart';

class AddGuestPage extends StatelessWidget {
  const AddGuestPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Add Guest')),
      body: const Center(child: Text('Add guest form coming soon')),
    );
  }
}
