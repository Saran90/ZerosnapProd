import 'package:flutter/material.dart';

class FrroFormPage extends StatelessWidget {
  const FrroFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('FRRO Registration Form')),
      body: const Center(child: Text('FRRO form coming soon')),
    );
  }
}
